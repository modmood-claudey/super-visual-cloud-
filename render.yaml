services:
  - type: web
    name: super-visual-cloud
    runtime: node
    buildCommand: cd server && npm install
    startCommand: node server/index.js
    envVars:
      - key: NODE_ENV
        value: production
